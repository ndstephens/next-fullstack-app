import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tickets',
};

export default function TicketsPage() {
  return (
    <section>
      <h2>TicketsPage</h2>
    </section>
  );
}
